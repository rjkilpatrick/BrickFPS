extends Spatial

# TODO: Add player if running scene standalone in the editor
#const PLAYER = preload("res://player/player.tscn")
#onready var players = $Players
#
#
#func _ready() -> void:
#	if OS.is_debug_build():
#		players.add_child(PLAYER.instance())


func _ready() -> void:
	pass
